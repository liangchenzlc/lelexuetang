
#微服务配置
spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      config:
        file-extension: yaml
        group: lelexuetang-project
        namespace: dev
        refresh-enabled: true
      username: nacos
      password: nacos
  profiles:
    active: dev
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml

